import { apiUserDataAccess } from './api-user-data-access';

describe('apiUserDataAccess', () => {
  it('should work', () => {
    expect(apiUserDataAccess()).toEqual('api-user-data-access');
  });
});
