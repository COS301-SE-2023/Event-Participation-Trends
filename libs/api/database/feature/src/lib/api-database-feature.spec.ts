import { apiDatabaseFeature } from './api-database-feature';

describe('apiDatabaseFeature', () => {
  it('should work', () => {
    expect(apiDatabaseFeature()).toEqual('api-database-feature');
  });
});
