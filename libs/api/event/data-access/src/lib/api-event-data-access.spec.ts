import { apiEventDataAccess } from './api-event-data-access';

describe('apiEventDataAccess', () => {
  it('should work', () => {
    expect(apiEventDataAccess()).toEqual('api-event-data-access');
  });
});
