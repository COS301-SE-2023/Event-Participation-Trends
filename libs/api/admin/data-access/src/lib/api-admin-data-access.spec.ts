import { apiAdminDataAccess } from './api-admin-data-access';

describe('apiAdminDataAccess', () => {
  it('should work', () => {
    expect(apiAdminDataAccess()).toEqual('api-admin-data-access');
  });
});
