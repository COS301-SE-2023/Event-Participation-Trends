import { apiEmailDataAccess } from './api-email-data-access';

describe('apiEmailDataAccess', () => {
  it('should work', () => {
    expect(apiEmailDataAccess()).toEqual('api-email-data-access');
  });
});
