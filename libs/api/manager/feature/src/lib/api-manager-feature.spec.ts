import { apiManagerFeature } from './api-manager-feature';

describe('apiManagerFeature', () => {
  it('should work', () => {
    expect(apiManagerFeature()).toEqual('api-manager-feature');
  });
});
