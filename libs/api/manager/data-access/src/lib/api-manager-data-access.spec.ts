import { apiManagerDataAccess } from './api-manager-data-access';

describe('apiManagerDataAccess', () => {
  it('should work', () => {
    expect(apiManagerDataAccess()).toEqual('api-manager-data-access');
  });
});
