import { appFloorEditorFeature } from './app-floor-editor-feature';

describe('appFloorEditorFeature', () => {
  it('should work', () => {
    expect(appFloorEditorFeature()).toEqual('app-floor-editor-feature');
  });
});
