import { appFloorEditorDataAccess } from './app-floor-editor-data-access';

describe('appFloorEditorDataAccess', () => {
  it('should work', () => {
    expect(appFloorEditorDataAccess()).toEqual('app-floor-editor-data-access');
  });
});
