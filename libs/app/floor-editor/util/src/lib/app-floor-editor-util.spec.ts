import { appFloorEditorUtil } from './app-floor-editor-util';

describe('appFloorEditorUtil', () => {
  it('should work', () => {
    expect(appFloorEditorUtil()).toEqual('app-floor-editor-util');
  });
});
