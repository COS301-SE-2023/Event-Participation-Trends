import { appManagerEventsFeature } from './app-manager-events-feature';

describe('appManagerEventsFeature', () => {
  it('should work', () => {
    expect(appManagerEventsFeature()).toEqual('app-manager-events-feature');
  });
});
