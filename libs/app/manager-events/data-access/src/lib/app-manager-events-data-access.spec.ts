import { appManagerEventsDataAccess } from './app-manager-events-data-access';

describe('appManagerEventsDataAccess', () => {
  it('should work', () => {
    expect(appManagerEventsDataAccess()).toEqual(
      'app-manager-events-data-access'
    );
  });
});
