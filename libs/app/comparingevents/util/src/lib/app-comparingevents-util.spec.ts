import { appComparingeventsUtil } from './app-comparingevents-util';

describe('appComparingeventsUtil', () => {
  it('should work', () => {
    expect(appComparingeventsUtil()).toEqual('app-comparingevents-util');
  });
});
