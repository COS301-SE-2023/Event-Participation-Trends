import { appComparingeventsDataAccess } from './app-comparingevents-data-access';

describe('appComparingeventsDataAccess', () => {
  it('should work', () => {
    expect(appComparingeventsDataAccess()).toEqual(
      'app-comparingevents-data-access'
    );
  });
});
