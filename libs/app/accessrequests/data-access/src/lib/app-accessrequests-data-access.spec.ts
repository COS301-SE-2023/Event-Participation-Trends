import { appAccessrequestsDataAccess } from './app-accessrequests-data-access';

describe('appAccessrequestsDataAccess', () => {
  it('should work', () => {
    expect(appAccessrequestsDataAccess()).toEqual(
      'app-accessrequests-data-access'
    );
  });
});
