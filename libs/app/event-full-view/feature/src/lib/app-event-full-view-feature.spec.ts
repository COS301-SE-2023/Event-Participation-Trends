import { appEventFullViewFeature } from './app-event-full-view-feature';

describe('appEventFullViewFeature', () => {
  it('should work', () => {
    expect(appEventFullViewFeature()).toEqual('app-event-full-view-feature');
  });
});
