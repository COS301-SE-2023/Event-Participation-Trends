import { appEventFullViewUtil } from './app-event-full-view-util';

describe('appEventFullViewUtil', () => {
  it('should work', () => {
    expect(appEventFullViewUtil()).toEqual('app-event-full-view-util');
  });
});
