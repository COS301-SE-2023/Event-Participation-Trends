import { appEventFullViewDataAccess } from './app-event-full-view-data-access';

describe('appEventFullViewDataAccess', () => {
  it('should work', () => {
    expect(appEventFullViewDataAccess()).toEqual(
      'app-event-full-view-data-access'
    );
  });
});
