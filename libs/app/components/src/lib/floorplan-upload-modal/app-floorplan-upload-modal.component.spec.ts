import { appFloorplanUploadModalComponent } from './app-floorplan-upload-modal.component';

describe('appFloorplanUploadModalComponent', () => {
    it('should work', () => {
        expect(appFloorplanUploadModalComponent()).toEqual(
            'app-floorplan-upload-modal-component'
        );
    });
});