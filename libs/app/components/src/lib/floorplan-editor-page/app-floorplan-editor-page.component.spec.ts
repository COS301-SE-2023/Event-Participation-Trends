import { appFloorplanEditorComponent } from './app-floorplan-editor-page.component';

describe('appFloorplanEditorComponent', () => {
    it('should work', () => {
        expect(appFloorplanEditorComponent()).toEqual(
            'app-floorplan-editor-component'
        );
    });
});