import { appVieweventsDataAccess } from './app-viewevents-data-access';

describe('appVieweventsDataAccess', () => {
  it('should work', () => {
    expect(appVieweventsDataAccess()).toEqual('app-viewevents-data-access');
  });
});
