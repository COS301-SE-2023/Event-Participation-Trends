import { appEventDetailsDataAccess } from './app-event-details-data-access';

describe('appEventDetailsDataAccess', () => {
  it('should work', () => {
    expect(appEventDetailsDataAccess()).toEqual(
      'app-event-details-data-access'
    );
  });
});
