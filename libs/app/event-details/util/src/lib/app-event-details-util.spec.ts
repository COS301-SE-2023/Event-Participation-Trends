import { appEventDetailsUtil } from './app-event-details-util';

describe('appEventDetailsUtil', () => {
  it('should work', () => {
    expect(appEventDetailsUtil()).toEqual('app-event-details-util');
  });
});
